# ========================================
# DIAGANA SCHOOL - VARIABLES D'ENVIRONNEMENT
# ========================================

# Serveur
NODE_ENV=development
PORT=3000

# Supabase (Base de données PostgreSQL)
# Récupérer depuis: https://app.supabase.com → Project Settings → API
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your_public_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Wasabi S3 (Stockage fichiers)
# Récupérer depuis: https://console.wasabisys.com → Access Keys
WASABI_ACCESS_KEY=your_access_key
WASABI_SECRET_KEY=your_secret_key
WASABI_BUCKET=diagana-school-files
WASABI_REGION=eu-west-2

# JWT (Sessions utilisateur)
# Générer une clé sécurisée: openssl rand -hex 64
JWT_SECRET=your-super-secure-secret-key-minimum-32-characters
JWT_EXPIRES_IN=7d

# Upload (Limites fichiers)
MAX_FILE_SIZE=50MB
ALLOWED_FILE_TYPES=pdf,docx,txt,jpg,jpeg,png,gif,mp4,avi,mov

# ========================================
# INSTRUCTIONS DE CONFIGURATION
# ========================================
#
# 1. Copier ce fichier: cp .env.example .env
# 2. Remplir les valeurs réelles dans .env
# 3. Ne JAMAIS commiter le fichier .env
#
# Pour Render.com:
# - Configurer ces variables dans Dashboard → Environment
# - NODE_ENV=production sera automatiquement défini
#
